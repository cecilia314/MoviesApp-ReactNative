import { View, Text } from 'react-native';
import React from 'react';

const MoviesContainer = () => {
  return (
    <View>
      <Text>MoviesContainer</Text>
    </View>
  );
};

export default MoviesContainer;
