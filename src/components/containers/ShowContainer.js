import { View, Text } from 'react-native';
import React from 'react';

const ShowContainer = () => {
  return (
    <View>
      <Text>ShowContainer</Text>
    </View>
  );
};

export default ShowContainer;
